<Window x:Class="WpfApp3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp3"
        mc:Ignorable="d"
        Background="Gray"
        Title="MainWindow" Height="450" Width="800" ResizeMode="NoResize"> 
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Content="Открыть картинку" x:Name="oneButton" Margin="10" Click="oneButton_Click"/>
            <Button Grid.Row="1" Content="Открыть окно" x:Name="nextButton" Margin="10" Click="nextButton_Click"/>
            <Button Grid.Row="2" Content="Открыть окно с кнопкой" x:Name="lastButton" Margin="10" Click="lastButton_Click"/>
        </Grid>
        <Frame  x:Name="mainFrame" Margin="30" Grid.Column="1"/>
        
    </Grid>
</Window>
